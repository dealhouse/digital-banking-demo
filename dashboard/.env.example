# Dashboard (Vite) environment variables
# Copy to .env.local for local development:
#   cp .env.example .env.local

# If you use a Vite proxy that forwards /api -> core-api, keep this "/api"
VITE_API_BASE=/api

# Direct core-api base URL (used by some endpoints in src/api.ts)
VITE_CORE_API_BASE=http://localhost:8080
